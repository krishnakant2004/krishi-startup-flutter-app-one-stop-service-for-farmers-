import 'package:flutter/material.dart';
import 'package:krishidost/main.dart';
import 'package:krishidost/utility/extensions.dart';

class FilterChoice extends StatelessWidget {
  const FilterChoice({super.key,required this.name,required this.index});
  final String name;
  final int index;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
